TODO
----
-Finalize average.test.cpp
-Fix storing of images as FITS files...
-IDEA: Extract test image generator function from unit tests - generate_test_image()
-Remove deprecated cmake message
-Replace "star recognizer" test images -> instead of one big, multiple small ones with only 2-5 stars.
-Make fits writer create specified directories if they do not exist
-Add unit test for remove_nans() - add parameter to allow specifying size of filter matrix
-Add hfd() to "actions"
-Add fwhm() to "actions" 
-IDEA: stretch(curve) function
-IDEA: Add histogram() function wich takes image and creates a hostogram
